<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- My Profile, should appear as action button if possible -->
    <item
        android:id="@+id/action_my_profile"
        android:title="@string/my_profile_action"
        app:showAsAction="never"/>

    <item
        android:id="@+id/action_show_only_active"
        android:title="@string/show_only_active"
        android:checkable="true"
        app:showAsAction = "never"/>

    <item
        android:id="@+id/surprise_me"
        android:title="@string/surprise_me"
        app:showAsAction = "never"/>


</menu>